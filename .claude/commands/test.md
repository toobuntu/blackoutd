---
description: Run the rspec behavioral test suite
---

Run `bundle exec rspec` and report the result.

If tests pass, give a one-line summary (count, time).

If tests fail, report:
1. The failing test names
2. The first failing assertion verbatim
3. The relevant file:line reference

Do not attempt to fix failures unless I explicitly ask. The goal is to
surface state, not to act on it.
