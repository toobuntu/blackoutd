BINARY       = blackoutd
AGENT_LABEL  = local.blackoutd
INSTALL_BIN  = /usr/local/bin/$(BINARY)
AGENT_DIR    = $(HOME)/Library/LaunchAgents
AGENT_PLIST  = $(AGENT_LABEL).plist
AGENT_DST    = $(AGENT_DIR)/$(AGENT_PLIST)
UID          = $(shell id -u)

SRCS   = main.m AppDelegate.m DisplayController.m
CC     = clang
CFLAGS = \
    -fobjc-arc \
    -Wall \
    -Wextra \
    -framework Cocoa \
    -framework CoreGraphics \
    -sectcreate __TEXT __info_plist Info.plist

.PHONY: all clean install uninstall reinstall load unload

all: $(BINARY)

$(BINARY): $(SRCS) AppDelegate.h DisplayController.h Info.plist
	$(CC) $(CFLAGS) -o $@ $(SRCS)
	codesign --sign - --force $(BINARY)

clean:
	rm -f $(BINARY)

# Build, install binary + plist, bootstrap agent.
install: $(BINARY)
	sudo install -d /usr/local/bin
	sudo install -m 755 $(BINARY) $(INSTALL_BIN)
	install -d $(AGENT_DIR)
	install -m 644 $(AGENT_PLIST) $(AGENT_DST)
	launchctl bootstrap gui/$(UID) $(AGENT_DST)

# Bootout, rebuild, reinstall binary only, re-bootstrap.
reinstall: unload clean all
	sudo install -m 755 $(BINARY) $(INSTALL_BIN)
	launchctl bootstrap gui/$(UID) $(AGENT_DST)

# Remove agent and binary.
uninstall: unload
	sudo rm -f $(INSTALL_BIN)
	rm -f $(AGENT_DST)

# Bootstrap the installed agent.
load:
	launchctl bootstrap gui/$(UID) $(AGENT_DST)

# Bootout the agent (uses agent label, not binary name).
unload:
	-launchctl bootout gui/$(UID)/$(AGENT_LABEL) 2>/dev/null
